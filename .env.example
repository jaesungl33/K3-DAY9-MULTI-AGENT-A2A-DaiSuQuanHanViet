# Optional: OpenAI-compatible endpoint for agent deliberation traces.
# Decisions always follow deterministic EC_POLICY_V1 rules.
# Model must be <= 10B parameters.
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.groq.com/openai/v1
# Model name is hard-coded in src/config.py (not in .env) for grading.
